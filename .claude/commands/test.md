---
description: Run the test suite (pytest) with concise output and stop on first failure.
---

Run `pytest -x -q` from the repo root. Report only failing tests with their tracebacks; if everything passes, reply with the one-line summary line from pytest.